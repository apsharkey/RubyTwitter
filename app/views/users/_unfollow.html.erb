<%= form_for(current_user.relationships.find_by_followed_id(@user),
             :html => { :method => :delete },
             :remote => true) do |f| %>
	<div class="actions"><%= link_to 'Unfollow', "#", :onclick=>"$('.edit_relationship').submit()", :class => "myButtonLink", :remote => :true %></div>
	
<% end %>